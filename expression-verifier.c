#include<stdio.h>
#include<string.h>
#include<ctype.h>

int main(int argc, char *argv[]){
    char* input = argv[1];
    int i=0,len=strlen(input);
    char term[2], oprts = {'+', '-', '*', '/'};
    for(i=0; i<len; i++){
        if (input[i] != '+')
    }
    return 0;
}
